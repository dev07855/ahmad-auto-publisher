name: scan-updates
on:
  schedule: [{ cron: '*/10 * * * *' }]
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: scan-updates
  cancel-in-progress: false
jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: pip install requests
      - working-directory: worker
        env:
          CHECKOVER_USER: ${{ secrets.CHECKOVER_USER }}
          CHECKOVER_PASS: ${{ secrets.CHECKOVER_PASS }}
          BRAIN_URL: ${{ secrets.BRAIN_URL }}
          ENQUEUE_SECRET: ${{ secrets.ENQUEUE_SECRET }}
          SCAN_LIMIT: '60'
          SCAN_PAGES: '3'
        run: python scan.py
